import Display from "@/components/main/page/page_components/display/display";

export default function Home() {
    const content = (
        <></>
    );

    return (
        <Display index={0} content={content} />
    );
}